<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--核心配置文件-->
<mapper namespace="king.drug.dal.mapper.TakeMapper">
    <insert id="insertOutStorage" parameterType="outstorageReqDTO">
        INSERT INTO outstorage(`medId`,`outStorageNum`,`patientId`,`outDate`)
        VALUE(#{medId},#{outStorageNum},#{patientId},#{outDate})
    </insert>
</mapper>